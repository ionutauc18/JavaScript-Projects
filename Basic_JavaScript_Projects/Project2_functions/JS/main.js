function myFunction() { //Adding a function
    var sentence = "I am from"; //Giving a variable to sentence  
    sentence += " Scornicesti, a city from Romania!"; //Concatenating a string using +=operator
    document.getElementById("demo").innerHTML = sentence; //Modifying the content of the HTMLelement
}